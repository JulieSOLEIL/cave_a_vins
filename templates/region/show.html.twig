{% extends 'base.html.twig' %}

{% block body %}
    <h1>Region</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">Identifiant</th>
                <th scope="col">Nom de la région</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ region.id }}</td>
                <td>{{ region.region }}</td>
            </tr>
        </tbody>
    </table>
    <br>
    <a class="btn btn-outline-secondary" href="{{ path('region_index') }}">Retour à la page précédente</a>
    <br><br>
    <a class="btn btn-outline-info" href="{{ path('region_edit', {'id': region.id}) }}">Modifier</a>
    <br><br>
    {{ include('region/_delete_form.html.twig') }}

{% endblock %}
